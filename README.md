# goNotes 📝

Легковесное, быстрое и современное self-hosted приложение для заметок с поддержкой Markdown, вложений и фокусом на приватность.

## ✨ Особенности

- **Markdown First**: Полноценная поддержка Markdown с продвинутой подсветкой синтаксиса и копированием кода в один клик.
- **Умное хранение медиа**: Автоматическая генерация превью для изображений с коррекцией ориентации (EXIF), встроенные плееры для видео и аудио.
- **Web Share Target (PWA)**: Приложение позволяет "шарить" ссылки и файлы прямо в goNotes со смартфона через стандартное меню "Поделиться".
- **Молниеносный поиск**: Поиск по тексту и тегам с поддержкой операторов (маска `*`) и мгновенной фильтрацией.
- **Self-Hosted & Portable**: Все данные хранятся в локальной базе SQLite. Приложение может быть собрано в один бинарный файл.
- **Клавиатурная навигация**: Полная поддержка Tab-навигации и горячих клавиш (например, `E` для быстрого редактирования выбранной заметки).

## 🚀 Технологический стек

- **Backend**: Go 1.25. Используется кастомный легковесный роутер и SQLite в режиме WAL для высокой производительности.
- **Frontend**: React 19, TypeScript 5, MUI 7 (Material UI).
- **Сборка**: Webpack 5 с Gzip-сжатием и автоматическим встраиванием ассетов в Go-бинарник.

## 🛠 Установка и запуск

### Требования
* Go 1.25+
* Node.js 20+ & npm

### 1. Сборка фронтенда
```bash
cd notes-ui
npm install
npm run release
```
Эта команда соберет оптимизированный Production-билд и скопирует его в директорию ассетов бэкенда.

### 2. Сборка бэкенда
```bash
./scripts/build.resources.sh
go build -o goNotes
```

### 3. Запуск с отладкой UI
```bash
./scripts/run.sh dev

cd notes-ui
npm run dev
```

После запуска приложение будет доступно по адресу `http://localhost:80` (настройки порта и адреса создаются автоматически в `config.json` при первом запуске).

## 📁 Структура проекта

- `/internal` — внутренняя логика бэкенда: роутер, работа с конфигурацией и утилиты.
- `/notes-ui` — исходный код фронтенд-приложения (React).
- `/assets` — статические файлы фронтенда, готовые к встраиванию в бинарный файл.
- `db.sql` — схема базы данных SQLite.

---
Лицензия: [MIT](LICENSE)
